{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install -g pnpm && pnpm install --ignore-scripts && pnpm --filter @workspace/conseil-chatbot run build && pnpm --filter @workspace/api-server run build"
  },
  "deploy": {
    "startCommand": "node artifacts/api-server/dist/index.mjs",
    "restartPolicyType": "ON_FAILURE"
  }
}